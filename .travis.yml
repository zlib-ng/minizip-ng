language: c

os:
    - linux

compiler:
    - clang
    - gcc

before_install:
    - sudo apt-get install libbsd-dev

before_script:
    - mkdir build
    - cd build
    - cmake .. -DBUILD_TEST=ON

script:
    - make
    - cd ../test
    - pip install numpy
    - python test.py --exe_dir=../build
