language: c

matrix:
    include:
        - os:
            - linux

compiler:
    - clang

before_script:  
    - mkdir build
    - cd build
    - cmake ..

script: make

after_success:
    - cd ../test
    - python test.py --exe_dir=../build
